{
  "credentials": [
    {
      "name": "tftBackendApi",
      "type": "httpHeaderAuth",
      "description": "Bearer token auth for backend API. Created in n8n UI Settings -> Credentials. Header Name: Authorization, Header Prefix: Bearer (with space), Header Value: API_SECRET_KEY from .env"
    }
  ]
}
