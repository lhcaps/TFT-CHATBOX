feat(phase-4-w2): Obsidian ingest pipeline with batch embedding and heading metadata

- config.py: add rag_chunk_size=2000 and rag_chunk_overlap=500 settings
- markdown.py: replace word-count splitter with char-based overlapping splitter
  (preserves paragraph > sentence > hard boundaries)
- ingest_obsidian.py: batch embed 16 chunks per Ollama call, store heading_path
  in metadata JSONB, skip unchanged files via content_hash
