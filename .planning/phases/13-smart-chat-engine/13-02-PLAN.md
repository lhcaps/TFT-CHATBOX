---
phase: "13-smart-chat-engine"
plan: "02"
type: "execute"
wave: 1
depends_on: ["13-01"]
files_modified:
  - "apps/frontend/src/components/ChampionProfile.tsx"
  - "apps/frontend/src/components/ItemCard.tsx"
  - "apps/frontend/src/components/TraitCard.tsx"
  - "apps/frontend/src/components/AugmentCard.tsx"
  - "apps/frontend/src/components/MessageList.tsx"
autonomous: true
requirements:
  - "SMART-02"
must_haves:
  truths:
    - "ChampionProfile card shows name, cost tier color, traits, ability"
    - "ItemCard shows name, category badge, stats, effect"
    - "TraitCard shows name, type, breakpoint count, champion list"
    - "AugmentCard shows name, tier color, effect"
    - "4-cost and 5-cost champions have glow pulse animation"
    - "Cards use cosmic accent theme on dark background"
  artifacts:
    - path: "apps/frontend/src/components/ChampionProfile.tsx"
      provides: "Champion entity card with cost tier + glow for 4/5-cost"
      exports: ["ChampionProfile"]
    - path: "apps/frontend/src/components/ItemCard.tsx"
      provides: "Item entity card with category badge"
      exports: ["ItemCard"]
    - path: "apps/frontend/src/components/TraitCard.tsx"
      provides: "Trait entity card with breakpoint highlight"
      exports: ["TraitCard"]
    - path: "apps/frontend/src/components/AugmentCard.tsx"
      provides: "Augment entity card with tier badge"
      exports: ["AugmentCard"]
  key_links:
    - from: "apps/frontend/src/components/MessageList.tsx"
      to: "ChampionProfile.tsx"
      via: "import and render by entity.type === 'champion'"
      pattern: "ChampionProfile"
    - from: "apps/frontend/src/components/CompCard.tsx"
      to: "card design patterns"
      via: "reference for cosmic theme, tier colors, glow effects"
      pattern: "tierStyle|glowShadow"
---

<objective>
Implement the four entity card components: ChampionProfile, ItemCard, TraitCard, AugmentCard. Each has distinct design per D-02 decisions. Replace the placeholder in MessageList with real components.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/frontend/src/components/CompCard.tsx — reference for cosmic theme, tier colors, glow shadows
@apps/frontend/src/components/MessageList.tsx — EntityCardPlaceholder at line ~118-138 (to replace)
@apps/frontend/src/api/types.ts — EntityCard, ChampionEntity, ItemEntity, TraitEntity, AugmentEntity types
@.planning/phases/13-smart-chat-engine/13-CONTEXT.md — D-02 Card Design System decisions

## Card Design System (from D-02)

**Theme:** Cosmic accents on minimal layout — `bg-gray-800/50`, `border-purple-500/30`, purple/blue hues, clean minimal card structure. "Đủ để biết là mùa gì nhưng vẫn ấn tượng."

**ChampionProfile:**
- Cost: tier-colored text (1=gray-400, 2=green-400, 3=blue-400, 4=purple-400, 5=yellow-400) — NO badge icons
- Trait badges: small colored pills (`bg-purple-900/50 text-purple-300 px-2 py-0.5`)
- Compact horizontal layout: `[Name] | [Trait] [Trait] | Ability: X`
- Glow: framer-motion glow pulse on border for 4-cost and 5-cost champions — 2.5s loop, border pulses between `border-purple-400` → `border-purple-300`

**ItemCard:**
- Category badge: color-coded (AD=red, AP=blue, Tank=green, Support=purple) — NO emoji
- Recipe shown as component names
- Stats + effect text

**TraitCard:**
- Type badge (Origin/Class)
- Tier count highlighted (e.g., "3/6/9 Anima")
- Champion list as small badges
- Active breakpoint highlighted

**AugmentCard:**
- Tier badge: Silver (gray-400), Gold (yellow-400), Prismatic (purple-400)
- Effect text
- Related champions if applicable

**All cards:** Dark theme matching `bg-gray-800/50`, `border-purple-500/30`, hover lift `hover:-translate-y-0.5`

## Reference Pattern from CompCard.tsx
```typescript
const TIER_STYLES = {
  S: { bg: 'bg-yellow-950/40', border: 'border-yellow-500/60', text: 'text-yellow-300', badge: 'text-yellow-400' },
};
const glowShadow = tier === 'S' ? '0 0 8px rgba(251,191,36,0.5)' : '';
```

## Cost Tier Colors (D-02)
- 1-cost: gray-400
- 2-cost: green-400
- 3-cost: blue-400
- 4-cost: purple-400
- 5-cost: yellow-400

## Item Category Colors (D-02)
- AD: red-400 (badge bg red-900/30)
- AP: blue-400 (badge bg blue-900/30)
- Tank: green-400 (badge bg green-900/30)
- Support: purple-400 (badge bg purple-900/30)

## Augment Tier Colors (D-02)
- Silver: gray-300
- Gold: yellow-400
- Prismatic: purple-400
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChampionProfile component with cost tier colors and glow</name>
  <files>apps/frontend/src/components/ChampionProfile.tsx</files>
  <read_first>apps/frontend/src/components/CompCard.tsx</read_first>
  <read_first>apps/frontend/src/api/types.ts</read_first>
  <action>
Create `apps/frontend/src/components/ChampionProfile.tsx` with the following implementation:

```typescript
import { motion } from 'framer-motion';
import type { ChampionEntity } from '../api/types';

const COST_COLORS: Record<number, string> = {
  1: 'text-gray-400',
  2: 'text-green-400',
  3: 'text-blue-400',
  4: 'text-purple-400',
  5: 'text-yellow-400',
};

const COST_GLOW_COLORS: Record<number, string> = {
  4: { from: 'rgba(168,85,247,0.5)', to: 'rgba(168,85,247,0.8)' },
  5: { from: 'rgba(250,204,21,0.5)', to: 'rgba(250,204,21,0.8)' },
};

interface ChampionProfileProps {
  champion: ChampionEntity;
  animate?: boolean; // set to false in non-streaming contexts
}

export function ChampionProfile({ champion, animate = true }: ChampionProfileProps) {
  const costColor = COST_COLORS[champion.cost] || 'text-gray-400';
  const glowConfig = COST_GLOW_COLORS[champion.cost];
  const hasGlow = !!glowConfig;

  const traitBadges = champion.traits.map((t, i) => (
    <span
      key={i}
      className="text-xs px-2 py-0.5 rounded bg-purple-900/50 text-purple-300 border border-purple-500/30 font-medium"
    >
      {t.name} {t.count}
    </span>
  ));

  const roleLabel = champion.role === 'carry' ? 'Carry' : champion.role === 'tank' ? 'Tank' : champion.role === 'support' ? 'Support' : 'Flex';

  const cardContent = (
    <div className="rounded-lg border border-purple-500/30 bg-gray-800/50 px-3 py-2 max-w-[380px] hover:-translate-y-0.5 transition-all">
      {/* Header: Name + Cost */}
      <div className="flex items-center gap-2 mb-1">
        <span className={`font-bold text-base ${costColor}`}>{champion.name}</span>
        <span className={`text-xs font-semibold ${costColor}`}>{champion.cost}-cost</span>
        <span className="text-xs text-gray-500 ml-auto">{roleLabel}</span>
      </div>

      {/* Traits */}
      {champion.traits.length > 0 && (
        <div className="flex flex-wrap gap-1 mb-1">
          {traitBadges}
        </div>
      )}

      {/* Ability */}
      {champion.ability && (
        <div className="text-xs text-gray-400">
          <span className="text-purple-400 font-medium">Ability:</span> {champion.ability}
        </div>
      )}
    </div>
  );

  // Only wrap with motion for 4-cost and 5-cost champions
  if (hasGlow && animate) {
    return (
      <motion.div
        animate={{
          borderColor: [glowConfig.from, glowConfig.to, glowConfig.from],
          boxShadow: [
            `0 0 4px ${glowConfig.from}`,
            `0 0 8px ${glowConfig.to}`,
            `0 0 4px ${glowConfig.from}`,
          ],
        }}
        transition={{
          duration: 2.5,
          repeat: Infinity,
          ease: 'easeInOut',
        }}
      >
        {cardContent}
      </motion.div>
    );
  }

  return cardContent;
}
```

Install framer-motion if not already present:
```bash
cd apps/frontend && npm install framer-motion
```

If framer-motion is already in package.json, skip the install step.
</action>
  <verify>
    <automated>grep -c "export function ChampionProfile" apps/frontend/src/components/ChampionProfile.tsx</automated>
  </verify>
  <done>ChampionProfile renders name with cost tier color, traits as badges, ability, glow animation for 4/5-cost</done>
</task>

<task type="auto">
  <name>Task 2: Create ItemCard component with category badge</name>
  <files>apps/frontend/src/components/ItemCard.tsx</files>
  <read_first>apps/frontend/src/api/types.ts</read_first>
  <action>
Create `apps/frontend/src/components/ItemCard.tsx`:

```typescript
import type { ItemEntity } from '../api/types';

const CATEGORY_STYLES: Record<string, { label: string; badge: string; badgeText: string; border: string; bg: string }> = {
  AD: {
    label: 'AD',
    badge: 'bg-red-900/40 text-red-300 border border-red-700/40',
    badgeText: 'text-red-400',
    border: 'border-red-600/30',
    bg: 'bg-gray-800/50',
  },
  AP: {
    label: 'AP',
    badge: 'bg-blue-900/40 text-blue-300 border border-blue-700/40',
    badgeText: 'text-blue-400',
    border: 'border-blue-600/30',
    bg: 'bg-gray-800/50',
  },
  Tank: {
    label: 'Tank',
    badge: 'bg-green-900/40 text-green-300 border border-green-700/40',
    badgeText: 'text-green-400',
    border: 'border-green-600/30',
    bg: 'bg-gray-800/50',
  },
  Support: {
    label: 'Support',
    badge: 'bg-purple-900/40 text-purple-300 border border-purple-700/40',
    badgeText: 'text-purple-400',
    border: 'border-purple-600/30',
    bg: 'bg-gray-800/50',
  },
};

interface ItemCardProps {
  item: ItemEntity;
}

export function ItemCard({ item }: ItemCardProps) {
  const style = CATEGORY_STYLES[item.category] || CATEGORY_STYLES.Support;

  return (
    <div className={`rounded-lg border ${style.border} ${style.bg} px-3 py-2 max-w-[360px] hover:-translate-y-0.5 transition-all`}>
      {/* Header: Name + Category badge */}
      <div className="flex items-center gap-2 mb-1">
        <span className="font-bold text-sm text-gray-100">{item.name}</span>
        <span className={`text-xs px-2 py-0.5 rounded font-bold ${style.badge}`}>
          {style.label}
        </span>
      </div>

      {/* Recipe */}
      {item.recipe && item.recipe.length > 0 && (
        <div className="text-xs text-gray-400 mb-1">
          <span className="text-gray-500">Recipe:</span>{' '}
          {item.recipe.join(' + ')}
        </div>
      )}

      {/* Stats */}
      {item.stats.length > 0 && (
        <div className="flex flex-wrap gap-1 mb-1">
          {item.stats.map((stat, i) => (
            <span key={i} className="text-xs text-green-400 bg-green-900/20 px-1.5 py-0.5 rounded font-mono">
              {stat}
            </span>
          ))}
        </div>
      )}

      {/* Effect */}
      {item.effect && (
        <div className="text-xs text-gray-300 leading-relaxed">
          {item.effect}
        </div>
      )}
    </div>
  );
}
```
</action>
  <verify>
    <automated>grep -c "export function ItemCard" apps/frontend/src/components/ItemCard.tsx</automated>
  </verify>
  <done>ItemCard renders name with category badge, recipe, stats, effect</done>
</task>

<task type="auto">
  <name>Task 3: Create TraitCard and AugmentCard components</name>
  <files>apps/frontend/src/components/TraitCard.tsx</files>
  <files>apps/frontend/src/components/AugmentCard.tsx</files>
  <read_first>apps/frontend/src/api/types.ts</read_first>
  <action>
Create `apps/frontend/src/components/TraitCard.tsx`:

```typescript
interface TraitCardProps {
  name: string;
  count: number;
  bonus: string;
}

export function TraitCard({ name, count, bonus }: TraitCardProps) {
  const breakpointColor = count >= 6 ? 'text-purple-400' : count >= 3 ? 'text-blue-400' : 'text-gray-400';

  return (
    <div className="rounded-lg border border-purple-500/30 bg-gray-800/50 px-3 py-2 max-w-[320px] hover:-translate-y-0.5 transition-all">
      {/* Header: Name + Count */}
      <div className="flex items-center gap-2 mb-1">
        <span className="font-bold text-sm text-gray-100">{name}</span>
        <span className={`text-xs font-bold ${breakpointColor}`}>{count}</span>
      </div>

      {/* Bonus text */}
      {bonus && (
        <div className="text-xs text-gray-300 leading-relaxed">
          {bonus}
        </div>
      )}
    </div>
  );
}
```

Create `apps/frontend/src/components/AugmentCard.tsx`:

```typescript
const TIER_STYLES: Record<string, { label: string; badge: string; border: string }> = {
  Silver: {
    label: 'Silver',
    badge: 'bg-gray-700/60 text-gray-300 border border-gray-600/40',
    border: 'border-gray-500/30',
  },
  Gold: {
    label: 'Gold',
    badge: 'bg-yellow-900/40 text-yellow-400 border border-yellow-600/40',
    border: 'border-yellow-500/30',
  },
  Prismatic: {
    label: 'Prismatic',
    badge: 'bg-purple-900/40 text-purple-400 border border-purple-600/40',
    border: 'border-purple-500/30',
  },
};

interface AugmentCardProps {
  name: string;
  tier: 'Silver' | 'Gold' | 'Prismatic';
  effect: string;
  relatedChampions?: string[];
}

export function AugmentCard({ name, tier, effect, relatedChampions }: AugmentCardProps) {
  const style = TIER_STYLES[tier] || TIER_STYLES.Silver;

  return (
    <div className={`rounded-lg border ${style.border} bg-gray-800/50 px-3 py-2 max-w-[380px] hover:-translate-y-0.5 transition-all`}>
      {/* Header: Name + Tier badge */}
      <div className="flex items-center gap-2 mb-2">
        <span className="font-bold text-sm text-gray-100">{name}</span>
        <span className={`text-xs px-2 py-0.5 rounded font-bold ${style.badge}`}>
          {style.label}
        </span>
      </div>

      {/* Effect */}
      {effect && (
        <div className="text-xs text-gray-300 leading-relaxed mb-2">
          {effect}
        </div>
      )}

      {/* Related champions */}
      {relatedChampions && relatedChampions.length > 0 && (
        <div className="flex flex-wrap gap-1">
          {relatedChampions.map((champ, i) => (
            <span
              key={i}
              className="text-[11px] px-1.5 py-0.5 rounded bg-blue-900/30 text-blue-300 border border-blue-700/30"
            >
              {champ}
            </span>
          ))}
        </div>
      )}
    </div>
  );
}
```
</action>
  <verify>
    <automated>grep -c "export function TraitCard" apps/frontend/src/components/TraitCard.tsx && grep -c "export function AugmentCard" apps/frontend/src/components/AugmentCard.tsx</automated>
  </verify>
  <done>TraitCard and AugmentCard implemented with correct styling</done>
</task>

<task type="auto">
  <name>Task 4: Wire entity cards into MessageList</name>
  <files>apps/frontend/src/components/MessageList.tsx</files>
  <read_first>apps/frontend/src/components/MessageList.tsx</read_first>
  <action>
Replace the `EntityCardPlaceholder` in `apps/frontend/src/components/MessageList.tsx` with the real components. Find the `EntityCardPlaceholder` function (added in SMART-01) and replace it with proper imports and rendering.

Update the imports at the top of the file:
```typescript
import { CompCard } from './CompCard';
import { ChampionProfile } from './ChampionProfile';
import { ItemCard } from './ItemCard';
import { TraitCard } from './TraitCard';
import { AugmentCard } from './AugmentCard';
```

Replace the `EntityCardPlaceholder` function with:

```typescript
function EntityCardRenderer({ entity }: { entity: EntityCard }) {
  switch (entity.type) {
    case 'champion':
      return <ChampionProfile champion={entity} />;
    case 'item':
      return <ItemCard item={entity} />;
    case 'trait':
      return <TraitCard name={entity.name} count={entity.count} bonus={entity.bonus} />;
    case 'augment':
      return (
        <AugmentCard
          name={entity.name}
          tier={entity.tier}
          effect={entity.effect}
          relatedChampions={entity.relatedChampions}
        />
      );
    default:
      // Unknown entity type — render as text
      return (
        <code className="block rounded border border-gray-600/40 bg-gray-800/50 px-2 py-1 my-1 text-xs font-mono text-gray-400">
          {JSON.stringify(entity)}
        </code>
      );
  }
}
```

Also update the `MessageContent` component to use `EntityCardRenderer` instead of `EntityCardPlaceholder`:
```typescript
if (block.kind === 'entity' && block.entity) {
  return <EntityCardRenderer key={i} entity={block.entity} />;
}
```
</action>
  <verify>
    <automated>grep -c "EntityCardRenderer" apps/frontend/src/components/MessageList.tsx</automated>
  </verify>
  <done>EntityCardRenderer imports real components, entity blocks render correctly by type</done>
</task>

</tasks>

<verification>
- ChampionProfile: `grep "export function ChampionProfile" apps/frontend/src/components/ChampionProfile.tsx` returns 1
- ItemCard: `grep "export function ItemCard" apps/frontend/src/components/ItemCard.tsx` returns 1
- TraitCard: `grep "export function TraitCard" apps/frontend/src/components/TraitCard.tsx` returns 1
- AugmentCard: `grep "export function AugmentCard" apps/frontend/src/components/AugmentCard.tsx` returns 1
- MessageList: `grep "EntityCardRenderer" apps/frontend/src/components/MessageList.tsx` returns 1
- Glow animation: `grep "motion.div" apps/frontend/src/components/ChampionProfile.tsx` returns 1
</verification>

<success_criteria>
All four entity card components render correctly. Champion glow animation works for 4-cost and 5-cost. Cost tier colors are correct. Category badges use correct colors. Cards have hover lift effect.
</success_criteria>

<output>
After completion, create `.planning/phases/13-smart-chat-engine/13-02-SUMMARY.md`
</output>
