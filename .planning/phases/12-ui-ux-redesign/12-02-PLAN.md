---
phase: 12-ui-ux-redesign
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - apps/frontend/src/components/CitationCard.tsx
  - apps/frontend/src/components/CitationModal.tsx
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "CitationCard truncates text by default and expands on click"
    - "Expanded citation shows in a modal overlay with full text"
    - "Source filter allows filtering citations by source type"
  artifacts:
    - path: apps/frontend/src/components/CitationCard.tsx
      provides: Expandable citation card with click handler
      min_lines: 1
    - path: apps/frontend/src/components/CitationModal.tsx
      provides: Modal overlay for expanded citation
      min_lines: 1
  key_links:
    - from: CitationCard.tsx
      to: CitationModal.tsx
      via: onClick prop triggers modal open
      pattern: selectedCitation state
---

<objective>
Upgrade CitationCard to v2 with modal expand, collapsible sections, and source filtering.

Purpose: Replace the `line-clamp-3` truncation with an interactive modal that shows full citation text on click. Users can also filter citations by source type.
Output: CitationCard.tsx (v2) + CitationModal.tsx (new), integrated into MessageList.tsx.
</objective>

<context>
@.planning/phases/12-ui-ux-redesign/12-CONTEXT.md
@.planning/phases/12-ui-ux-redesign/12-UI-SPEC.md
@apps/frontend/src/components/CitationCard.tsx
@apps/frontend/src/api/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CitationModal component (D-02)</name>
  <files>apps/frontend/src/components/CitationModal.tsx</files>
  <action>
Create a new file: `apps/frontend/src/components/CitationModal.tsx`

This is a modal overlay that displays the full citation text when a CitationCard is clicked.

```tsx
interface CitationModalProps {
  citation: {
    id: string;
    source: string;
    heading: string;
    text: string;
    score: number;
  } | null;
  onClose: () => void;
}

export function CitationModal({ citation, onClose }: CitationModalProps) {
  if (!citation) return null;

  const percent = Math.round(citation.score * 100);

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm"
      onClick={onClose}
    >
      <div
        className="bg-gray-800 border border-gray-600 rounded-2xl p-6 max-w-2xl w-full mx-4 max-h-[80vh] overflow-y-auto"
        onClick={(e) => e.stopPropagation()}
      >
        {/* Header */}
        <div className="flex items-start justify-content-between gap-4 mb-4">
          <div className="flex-1 min-w-0">
            <span className="text-xs font-mono text-purple-400 block truncate" title={citation.source}>
              {citation.source}
            </span>
            {citation.heading && (
              <span className="text-xs text-gray-500 block truncate mt-1" title={citation.heading}>
                {citation.heading}
              </span>
            )}
          </div>
          <div className="flex items-center gap-3 shrink-0">
            <span className="text-xs text-gray-500">{percent}% match</span>
            <button
              onClick={() => {
                navigator.clipboard.writeText(`${citation.source}\n${citation.heading}\n${citation.text}`);
              }}
              className="text-gray-400 hover:text-gray-200 transition-colors text-xs"
              title="Copy citation"
            >
              Copy
            </button>
            <button
              onClick={onClose}
              className="text-gray-500 hover:text-white transition-colors"
              aria-label="Close modal"
            >
              <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          </div>
        </div>

        {/* Full citation text */}
        <div className="text-sm text-gray-300 whitespace-pre-wrap leading-relaxed">
          {citation.text}
        </div>
      </div>
    </div>
  );
}
```

Key design decisions:
- `fixed inset-0 z-50` for full-screen overlay
- `bg-black/60 backdrop-blur-sm` for backdrop effect
- `onClick={onClose}` on backdrop only (clicking content doesn't close)
- `max-h-[80vh] overflow-y-auto` for scrollable content
- Copy button uses `navigator.clipboard.writeText()`
- No external library — pure React + Tailwind
  </action>
  <verify>
test -f apps/frontend/src/components/CitationModal.tsx && echo "EXISTS"
# Verify file exists and contains expected elements
grep -n "CitationModal\|bg-black/60\|backdrop-blur-sm\|navigator.clipboard" apps/frontend/src/components/CitationModal.tsx
  </verify>
  <done>
CitationModal.tsx created with backdrop overlay, full citation text, copy button, and close functionality.
</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CitationCard.tsx to v2 (D-02)</name>
  <files>apps/frontend/src/components/CitationCard.tsx</files>
  <action>
Read CitationCard.tsx first.

Rewrite CitationCard.tsx to v2. The card shows truncated info by default and calls onClick when clicked.

Changes from current version:
1. Remove `line-clamp-3` — instead show first ~200 chars + "..." + "Read more"
2. Add cursor-pointer hover effect
3. Add `onClick` prop — call it when card is clicked
4. Remove the Card component wrapper (use native divs for simpler structure)
5. Keep source filter logic for MessageList-level filtering (Task 3)

```tsx
import type { Citation } from '~/api/types';

interface CitationCardProps {
  citation: Citation;
  onClick?: () => void;
}

export function CitationCard({ citation, onClick }: CitationCardProps) {
  const percent = Math.round(citation.score * 100);

  // Truncate text to ~200 chars
  const truncatedText = citation.text.length > 200
    ? citation.text.slice(0, 200).trimEnd() + '...'
    : citation.text;

  return (
    <div
      onClick={onClick}
      className="cursor-pointer hover:border-gray-600 transition-colors border border-gray-700 rounded-xl p-3 bg-gray-800/50"
    >
      <div className="flex items-center justify-between gap-2 mb-1">
        <span className="text-xs font-mono text-gray-400 truncate flex-1" title={citation.source}>
          {citation.source}
        </span>
        <span className="text-xs text-gray-500 shrink-0">{percent}%</span>
      </div>
      {citation.heading && (
        <div className="text-xs text-gray-500 truncate mb-1" title={citation.heading}>
          {citation.heading}
        </div>
      )}
      <p className="text-sm text-gray-300">{truncatedText}</p>
      {citation.text.length > 200 && (
        <div className="text-xs text-purple-400 mt-1">Click to read more</div>
      )}
    </div>
  );
}
```

Key design:
- Uses native divs (not shadcn Card) — lighter weight, custom styling
- Truncates at 200 chars with "..." and "Click to read more"
- onClick prop for modal trigger
- hover:border-gray-600 for visual feedback
- No copy button on collapsed card (only in modal)
  </action>
  <verify>
grep -n "cursor-pointer\|Click to read more\|onClick\|truncatedText" apps/frontend/src/components/CitationCard.tsx
# Must find: cursor-pointer, onClick prop, truncation logic, "Click to read more"
  </verify>
  <done>
CitationCard.tsx v2 created with click-to-expand behavior, 200-char truncation, and onClick handler.
</done>
</task>

<task type="auto">
  <name>Task 3: Integrate CitationCard v2 + source filter into MessageList (D-02)</name>
  <files>apps/frontend/src/components/MessageList.tsx</files>
  <action>
Read MessageList.tsx first.

Update the citations section in MessageList.tsx:

1. **Add state for modal:** Import `useState` from react, import `CitationModal` from './CitationModal'.

2. **Add selectedCitation state inside MessageList component:**
   ```tsx
   const [selectedCitation, setSelectedCitation] = useState<{
     id: string; source: string; heading: string; text: string; score: number;
   } | null>(null);
   ```

3. **Add source filter state:**
   ```tsx
   const [sourceFilter, setSourceFilter] = useState<string>('all');
   // Source options: 'all' | 'obsidian' | 'patch' | 'metatft'
   ```

4. **Filter citations before rendering:**
   ```tsx
   const filteredCitations = msg.citations?.filter((c) => {
     if (sourceFilter === 'all') return true;
     const s = c.source.toLowerCase();
     if (sourceFilter === 'obsidian') return s.includes('obsidian') || (!s.includes('patch') && !s.includes('metatft'));
     if (sourceFilter === 'patch') return s.includes('patch');
     if (sourceFilter === 'metatft') return s.includes('metatft');
     return true;
   }) ?? [];
   ```

5. **Add source filter UI above citation grid:**
   ```tsx
   <div className="flex gap-2 mb-2">
     {(['all', 'obsidian', 'patch', 'metatft'] as const).map((s) => (
       <button
         key={s}
         onClick={() => setSourceFilter(s)}
         className={`text-xs px-2 py-1 rounded ${
           sourceFilter === s
             ? 'bg-purple-600 text-white'
             : 'bg-gray-700 text-gray-400 hover:bg-gray-600'
         }`}
       >
         {s === 'all' ? 'All' : s.charAt(0).toUpperCase() + s.slice(1)}
       </button>
     ))}
   </div>
   ```

6. **Update CitationCard usage:**
   ```tsx
   <CitationCard
     key={c.id}
     citation={c}
     onClick={() => setSelectedCitation(c)}
   />
   ```

7. **Add CitationModal at end of component:**
   ```tsx
   <CitationModal
     citation={selectedCitation}
     onClose={() => setSelectedCitation(null)}
   />
   ```

Make sure `filteredCitations` is used instead of `msg.citations` in the map.
  </action>
  <verify>
grep -n "CitationModal\|selectedCitation\|sourceFilter\|filteredCitation" apps/frontend/src/components/MessageList.tsx
# Must find: CitationModal import, selectedCitation state, sourceFilter state, filteredCitations usage, CitationModal rendered at end
  </verify>
  <done>
MessageList.tsx integrates CitationCard v2 with modal expand, source filter buttons, and CitationModal overlay.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side | Modal is purely client-side rendering; no data exfiltration |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-12-02-01 | Information | Citation text in modal | accept | Citations are TFT game data, no sensitive info |
| T-12-02-02 | XSS | Citation source text rendered | mitigate | React escapes text by default; use textContent for source if concerned |
| T-12-02-03 | Privacy | Clipboard copy | accept | User intentionally copies citation text |

</threat_model>

<verification>
1. Send a message that returns citations in the assistant response
2. Click a CitationCard — verify modal opens with full text
3. Click backdrop or close button — verify modal closes
4. Click "Copy" in modal — verify text is in clipboard
5. Use source filter buttons — verify citations filter correctly
</verification>

<success_criteria>
- CitationCard truncates at 200 chars with "Click to read more"
- Clicking card opens modal with full citation text
- Modal has copy button and close button
- Source filter buttons (All / Obsidian / Patch / MetaTFT) filter the citation grid
- Modal backdrop click closes the modal
</success_criteria>
