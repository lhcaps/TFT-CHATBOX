---
phase: 12-ui-ux-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/ChatShell.tsx
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "Sidebar is hidden on mobile and appears as a slide-in drawer"
    - "Drawer opens via hamburger menu button and closes via backdrop or X button"
    - "Chat content area uses full width on mobile"
  artifacts:
    - path: apps/frontend/src/components/ChatShell.tsx
      provides: Responsive sidebar drawer with mobile navigation
      min_lines: 1
  key_links:
    - from: ChatShell.tsx
      to: useState for sidebarOpen
      via: Mobile hamburger button
      pattern: sidebarOpen state
---

<objective>
Add responsive breakpoints to ChatShell: sidebar becomes a slide-in drawer on mobile with hamburger menu toggle.

Purpose: On mobile (<768px), the sidebar is hidden by default and appears as a drawer when the user taps a hamburger button. On tablet, sidebar is collapsible. On desktop, sidebar stays fixed.
Output: Updated ChatShell.tsx with responsive drawer, hamburger button, and backdrop overlay.
</objective>

<context>
@.planning/phases/12-ui-ux-redesign/12-CONTEXT.md
@.planning/phases/12-ui-ux-redesign/12-UI-SPEC.md
@apps/frontend/src/components/ChatShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile drawer to ChatShell (D-04)</name>
  <files>apps/frontend/src/components/ChatShell.tsx</files>
  <action>
Read ChatShell.tsx first.

This task modifies ChatShell.tsx to add responsive drawer behavior. Do NOT modify MessageList.tsx, Composer.tsx, or any other files in this plan.

**Step 1: Add state and imports**

Add at the top of ChatShell component:
```tsx
const [sidebarOpen, setSidebarOpen] = useState(false);
```

**Step 2: Add hamburger button to header**

Find the header section. Add a hamburger button BEFORE the session title (visible only on mobile):
```tsx
<header className="flex-shrink-0 flex items-center px-4 md:px-6 py-3 border-b border-gray-700 bg-gray-800 gap-4">
  {/* Hamburger — mobile only */}
  <button
    className="md:hidden text-gray-400 hover:text-white p-1 -ml-1"
    onClick={() => setSidebarOpen(true)}
    aria-label="Open sidebar"
  >
    <svg className="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
    </svg>
  </button>
```

**Step 3: Make sidebar responsive**

Wrap the existing `<aside>` element with responsive visibility:
```tsx
{/* Desktop sidebar (always visible) */}
<aside className="hidden md:flex w-64 shrink-0 flex-col border-r border-gray-700 bg-gray-800">
  {/* ... existing sidebar content (keep everything inside) ... */}
</aside>

{/* Mobile drawer (shown when sidebarOpen) */}
{sidebarOpen && (
  <div className="md:hidden fixed inset-0 z-50 flex">
    {/* Backdrop */}
    <div
      className="absolute inset-0 bg-black/60"
      onClick={() => setSidebarOpen(false)}
    />
    {/* Drawer */}
    <aside className="relative z-10 w-64 shrink-0 flex flex-col border-r border-gray-700 bg-gray-800 transform transition-transform duration-300 ease-out"
      style={{ transform: sidebarOpen ? 'translateX(0)' : 'translateX(-100%)' }}
    >
      {/* Drawer header with close button */}
      <div className="flex items-center justify-content-between p-4 border-b border-gray-700">
        <span className="text-sm font-semibold text-gray-300">Sessions</span>
        <button
          onClick={() => setSidebarOpen(false)}
          className="text-gray-400 hover:text-white p-1"
          aria-label="Close sidebar"
        >
          <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>

      {/* New Chat button */}
      <div className="p-4 border-b border-gray-700">
        <Button onClick={onNewSession} className="w-full">
          <Plus className="w-4 h-4" />
          New Chat
        </Button>
      </div>

      {/* Session list */}
      <div className="flex-1 overflow-y-auto space-y-1 p-2">
        {loading && sessions.length === 0 ? (
          <p className="px-3 py-2 text-gray-500 text-sm">Loading...</p>
        ) : sessions.length === 0 ? (
          <p className="px-3 py-2 text-gray-500 text-sm">No sessions yet</p>
        ) : (
          sessions.map((s) => (
            <div
              key={s.id}
              className={`group flex items-center gap-2 rounded-xl px-3 py-2 cursor-pointer text-sm transition-colors
                ${currentSession?.id === s.id ? 'bg-gray-700 text-white' : 'text-gray-400 hover:bg-gray-700 hover:text-white'}`}
              onClick={() => {
                onSelectSession(s.id);
                setSidebarOpen(false);
              }}
            >
              <span className="flex-1 truncate">{s.title ?? 'New conversation'}</span>
              <span className="flex items-center gap-1 shrink-0">
                <span className="text-xs bg-gray-600 px-1.5 py-0.5 rounded">{s.mode}</span>
                <Button
                  variant="ghost"
                  size="icon"
                  onClick={(e: React.MouseEvent) => {
                    e.stopPropagation();
                    onDeleteSession(s.id);
                  }}
                  title="Delete session"
                  className="text-gray-500 hover:text-red-400 h-7 w-7"
                >
                  <Trash2 className="w-3.5 h-3.5" />
                </Button>
              </span>
            </div>
          ))
        )}
      </div>
    </aside>
  </div>
)}
```

**Step 4: Ensure main content uses full width on mobile**

The main chat area (`<div className="flex-1 flex flex-col min-w-0">`) should work correctly with the conditional drawer — since the drawer is outside the flex container, the main area naturally uses full width on mobile. Confirm the className is:
```tsx
<div className="flex-1 flex flex-col min-w-0">
```

**Key points:**
- `hidden md:flex` on desktop sidebar = sidebar always shown on desktop
- `md:hidden` on hamburger = button only shown on mobile
- Drawer is `fixed inset-0 z-50` = full screen overlay
- `transform translateX` for slide animation
- Clicking backdrop closes drawer
- Clicking a session in drawer closes drawer and selects session
- 300ms ease-out transition for smooth slide
  </action>
  <verify>
grep -n "sidebarOpen\|md:hidden\|hidden md:flex\|z-50\|translateX\|backdrop" apps/frontend/src/components/ChatShell.tsx
# Must find: sidebarOpen state, md:hidden (hamburger), hidden md:flex (desktop sidebar), z-50, translateX, backdrop
# Must find: setSidebarOpen calls
  </verify>
  <done>
ChatShell.tsx has responsive drawer: hamburger button on mobile, sidebar fixed on desktop, slide-in drawer with backdrop on mobile.
</done>
</task>

<task type="auto">
  <name>Task 2: Fix Header layout — responsive flex (D-04)</name>
  <files>apps/frontend/src/components/ChatShell.tsx</files>
  <action>
Read ChatShell.tsx first.

Fix the fragile `w-64` fixed width on ModeTabs in the header. This task modifies ONLY the header section of ChatShell.tsx — the drawer and sidebar are handled by Task 1.

1. **Replace the fixed-width wrapper around ModeTabs:** Find and change:
   ```
   <div className="w-64">
     <ModeTabs value={currentMode} onChange={onModeChange} />
   </div>
   ```
   To:
   ```
   <div className="flex items-center gap-3">
     <ModeTabs value={currentMode} onChange={onModeChange} />
   </div>
   ```

2. **Ensure header itself uses responsive flex:** Find the header div (the one with the hamburger button added in Task 1). Ensure it has responsive padding and gap:
   ```
   <header className="shrink-0 flex items-center px-4 md:px-6 py-3 border-b border-gray-700 bg-gray-800 gap-4">
   ```
   - `px-4 md:px-6`: reduced padding on mobile, full padding on desktop
   - `gap-4`: space between elements

3. **Session title truncate:** Ensure the title div has `flex-1 truncate` so it takes remaining space and truncates overflow.

No other changes — drawer and sidebar are in Task 1.
  </action>
  <verify>
grep -n "w-64\|flex items-center gap-3\|gap-4\|px-4 md:px-6" apps/frontend/src/components/ChatShell.tsx
# Must NOT find: w-64 (removed from header)
# Must find: flex items-center gap-3 and responsive px padding on header
  </verify>
  <done>
Header uses flexbox with gap instead of fixed w-64. Mode tabs scale gracefully across breakpoints.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan is pure UI/navigation — no data flows across trust boundaries.

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-12-04-01 | N/A | Drawer navigation | accept | Pure UI navigation, no data or security implications |

</threat_model>

<verification>
1. Open browser at 375px width (mobile)
2. Verify sidebar is hidden, hamburger button is visible in header
3. Tap hamburger — verify drawer slides in from left
4. Tap backdrop — verify drawer closes
5. Switch to 1280px (desktop) — verify sidebar is always visible, hamburger is hidden
6. Switch to 900px (tablet) — verify sidebar visible (md breakpoint), hamburger hidden
</verification>

<success_criteria>
- Mobile (<768px): Hamburger visible, sidebar hidden, drawer slides in on tap
- Tablet/Desktop (≥768px): Hamburger hidden, sidebar always visible
- Drawer: 300ms slide animation, backdrop closes drawer
- Session selection in drawer closes drawer
- Main chat area uses full width on mobile
</success_criteria>
